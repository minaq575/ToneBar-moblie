name: week4action
description: A new Flutter project.
version: 0.1.0

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  audioplayers: ^4.1.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/sound/xylophone/